import { BestSellerProduct } from './best-seller-product.model';

describe('BestSellerProduct', () => {
  it('should create an instance', () => {
    expect(new BestSellerProduct()).toBeTruthy();
  });
});
